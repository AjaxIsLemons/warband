<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Authored content is part of the live Unity runtime. Keep the same ceiling as
         Warband.Sim / Warband.Run so all three assemblies can drop into Unity 6. -->
    <TargetFramework>netstandard2.1</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>9</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../Warband.Sim/Warband.Sim.csproj" />
    <ProjectReference Include="../Warband.Run/Warband.Run.csproj" />
  </ItemGroup>
</Project>
