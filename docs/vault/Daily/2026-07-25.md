# 2026-07-25 — overnight research: fight legibility plan

Research-only session (Jake, before bed: "make the fights render more legible — plan, no
impl, spawn agents, questions in the morning"). Three parallel research agents: render-layer
inventory (line-level), autobattler legibility (TFT/Underlords/Auto Chess/HSBG/SAP/
Mechabellum/Backpack Battles/LTD2, sourced), asset/animation/VFX pipeline survey.

**Output: `Design/fight-legibility.md`** — the proposal for roadmap item 1. Highlights:

- Reframed "not clear what's happening" into three failure modes (can't see / can't
  attribute / can't infer the rule); the genre fixed the first two and never the third —
  our deterministic sim can (first-party win-probability re-sim, damage charts).
- Inventory found real regressions: active volume profile lost DoF/ColorAdjustments (the
  diorama tilt-shift is NOT running; tuning knobs dead), MSAA off, Game/Boot scenes
  untracked, silhouettes keyed on Name substring instead of the ChassisId replay v3 added.
- Cheap unlock confirmed: `Cast` events carry no ability identity, but the fold has
  ChassisId/Traits and the dispatcher holds the caster — a `byChassis` tell filter = 8
  distinct cast looks, zero sim change.
- Silent vocabulary quantified: 23/27 statuses invisible beyond a grey pip, 12/20 event
  kinds tell-less (CheatDeath — a hero moment — renders as nothing).
- Pipeline verdict: KayKit (shared rig, 161 CC0 combat animations, $0→$150) over AI
  generation for the roster; Unity AI tokens go to gap animations (text-to-motion spear
  thrust/banner plant), VFX textures/sigils, icons, SFX stings. Tripo/Hunyuan 3D+rigging
  confirmed live in our editor MCP for later bespoke enemy families.
- Genre laws adopted into the plan: spectator retiming (TFT slowed everything), Riot's
  impact-proportionality table (defensives QUIET, autos small), Underlords' mana-bar
  color-flip at full, status-as-material, segmented HP bars, audio as the free channel.
- **Guildrun is real** — Leyline's PvE hex roguelike autobattler, our thesis almost exactly,
  demo #1 on Steam, late-2026 launch. Worth a demo session.

Board updated (item 1 carries the plan pointer + six decision points). Briefing item could
NOT be queued (PORTAL_AGENT_TOKEN missing in this session — flagged to Jake).

## Morning: Jake approved — build session (same day)
Jake: *"sold on everything but kaykit — find a free/cheaper alternative; otherwise build
everything."* Built and committed, all editor-verified via contact sheets, console clean:
- **Phase 0** (`acddbf0`): post stack repaired (DoF/ColorAdjustments restored as real
  sub-assets — script-added volume components need `AddObjectToAsset` or they die on domain
  reload), MSAA 4×, SMAA, scenes tracked. DoF band 18-30 measured against real distances
  (units 10.7-15.1, far rim 18). Editor had been left in Play Mode overnight — exited first.
- **byChassis casts + ChassisId silhouettes** (`f788491`): TellMatch chassis filter
  (sim-tested, view-context law same as ranged), TellDef byChassis/byFlavor, 8 authored
  per-chassis cast tells, silhouettes keyed on ChassisId (Name fallback). Zero silhouette
  fallbacks across fixtures.
- **Phase 1 grammar** (`a1fcf8b`): beat sequencer (causal-chain stagger inside a tick's
  beat) + hit-stop (playhead hold on Death/crit; Director keeps stepping — FX animate
  through the freeze) · mana-ready color flip + pulse · segmented ally/enemy HP bars ·
  status-as-material torso tints (control grey / Phase ice / burn ember) · full 27-kind
  StatusColor map · registry fills incl. CheatDeath hero moment + byFlavor FieldCreated.
- **Phase 4 sim** (`113a2de`, subagent): FightSummary + BattleForecast, 299 tests green.
  Attribution gotchas documented in the commit (Death never sets Root; killing-blow credit).
- **Unverified-by-machine:** beat stagger + hit-stop are temporal — need Jake's live play
  eyeball. GenerateAsset (SFX stings etc.) needs a consent turn before first use.
- **End-fight readout** (`40eb076`): FightSummary-driven — damage shares (% of team) +
  "X fell to Y at N.Ns" story lines + kills. Verified via end-tick capture.
- **KayKit verdict + Phase 2 slice** (`82b7a6b`): free tier beats everything at $0 — same
  shared Rig_Medium skeleton + 173 CC0 clips as the $150 bundle (agent parsed the files:
  identical 23-joint set across all bodies/animations/skeletons; Quaternius has no bow
  anims, Kenney has no skeletons). **Real minis now render on the board**: 6 bodies,
  handslot kitbash props (spear/banner/hammer), shared Idle↔Walk controller, team ground
  discs (models flash off white — no texture tinting), primitive fallback intact. Gotchas
  learned: itch.io headless download needs the POST /file/{id} + CSRF path (downloader
  scripts in session scratchpad) · script-created AnimatorController via MCP works fine ·
  Animator must be manually Update()-sampled for edit-mode captures, guarded on dead units'
  inactive roots. Next slice: Attack/Cast/Hit/Death crossfades from the tell director
  (clips already imported), cleric body ($7.95 EXTRA adds Druid if Mage-reuse reads poorly).

## Afternoon round 2 — Jake: "approve credit spend; portraits clash with this style; GOGOGO"
(`b3898e8`) Attack/Cast **combat crossfades** wired from the director: per-weapon-class KayKit
clip fired at each tell's StartAt, `Animator.speed` fitted so the swing fills windup+motion
(render-contract §4); ActionSpeed per-state param; exits back to Idle. **9 ElevenLabs SFX
stings** generated in-editor and wired via TellDef `sound`/`critSound` (played at impact with
the flash; missing clip = silent no-op). **Style-clash bridge, both directions:** KayKit
atlases recolored to the grim Last-Hour palette (UV-safe HSV remap; originals in scratchpad;
knobs: sat 52 / bright 76 / colorize 12) + two bridge-style test portraits (stylized-3d model
with original portraits as references) in `Art/PortraitTests/` for Jake to judge before all 8
are batched. Hit/Death crossfades deferred — the fold hides corpses the same tick they die,
so death anims need a corpse-linger design decision first. Audio/anim feel = live-play items.

## Evening: three play-note rounds fixed, then the combat-spectacle plan
Play-note rounds (through `9b4f861`): text sharpness (font texture res 180/200) · banner
prop scaled · hexSize/tileScale into tuning (`board` group, live rebuild) · battle-speed
persistence (PlaybackTune owns ticksPerSecond; scene's baked 10000 reset to 10) · off-board
text blur root-caused to transparent TextMesh writing no depth under DoF → **DoF defaulted
off** (`post.dofEnabled`) · click-T-pose + lock-in teleport = shell rebuilds rendering an
unplaced first frame → Build() now snaps positions and samples animators.

**Then Jake: "improve combat rendering significantly — casting system, aura/hex effects,
status icons w/ time+stacks, better attack VFX, propose more. GO BIG."** Four agents
(2 inventory, 2 design) → **`Design/combat-spectacle.md`** (palette law + T0-T3 intensity
tiers, 4-beat cast grammar + 8 era sigils, per-signature specs for every chassis + key
overrides, field/status/attack/rider language, 10 ranked go-big proposals + killed
darlings, ~60-gen asset manifest) + **`Design/fx-runtime.md`** (VfxLibrary C# recipes,
Director-stepped `Simulate()` particles seeded from (tick,unitId), 6 hand-HLSL URP shaders,
hex ground substrate, status icon row w/ radial clock-sweep, corpse linger + dissolve,
build phases P0-P6).

**Inventory found a real shipped bug:** the playback fold DIVERGES from sim truth at the
first Burn tick of any fight (Burn merges into one pool sim-side and decays with no event;
the fold appends instances and matches expiry on kind+Mag — magnitude freezes, the icon
never clears). Affects castfest/statusstorm/glyphwar/skirmish. No guardrail fixture used
Burn — that's how it shipped. Fix spec'd as P0. Also measured: `Cause.Trigger` is the
2nd-most-common damage cause (110 vs Ability's 20 in the test corpus) and has NO tell —
every Detonate/thorns/splitheads rider renders as plain damage today.

Awaiting Jake: go-big menu selection + asset-batch approval (decision points in
combat-spectacle.md). Replay v5 + Burn fix flagged as mechanical/correctness.

## Management Hall — Hourstone Table implementation

Jake rejected the generic left Management drawer and approved the spatial 2.5D table plan, with
landscape phones and tablets promoted to acceptance targets. ADR 0021 records the result.

- Replaced the drawer/card dump with stable Breach / Market / Warband / Armory / Hourstone
  geography, directional station workspaces, attention receipts, safe-area layouts, touch targets,
  and phone side-sheet dossiers.
- Added pure before/after run mutation planning. Required spec/boss choices outrank acquisitions;
  ordinary wins route through the refreshed Market.
- Fight completion and Skip now share a blocking FightSummary-driven result gate. Watch Again
  replays the stored battle and cannot resolve or pay twice.
- Armory selection previews composed HP, basic power, reach, cadence, and crit before→after.
- Added reduced-motion choreography, presentation timing config, semantic polish cues, a
  nonpersistent run-conclusion seam, editor hooks, and an F2 Hall Flow Lab.

## Management Hall polish research and proposal

- Audited the new Hourstone Table, workspaces, result gate, responsive captures, motion config,
  and semantic cue seam. The architecture/clarity is strong; visual identity and causal
  transaction feedback remain programmer-grade.
- Researched current Unity UI Toolkit performance/custom-vector guidance, mobile targets and
  haptics, motion/focus accessibility, and reference patterns from Into the Breach, Balatro,
  The Bazaar, Inscryption, and Darkest Dungeon.
- Proposed `Design/hall-polish.md`: the Hall as an obsidian Tower instrument with living Sand,
  one semantic palette, station-specific geometry/motion rather than station colours, explicit
  preview→selection→commit states, causal Buy/Reroll/Equip/Bind/Result sequences, a reusable
  feedback bus/motion director/Painter2D FX layer/audio+haptic sinks, and a data-driven animated
  Rule Preview control.

## Management Hall polish foundation — approved and built

Jake approved the direction and emphasized deep selection feedback, first-reveal choreography,
smooth transitions, reusable systems, and debug-menu tuning.

- Built a hot-reloadable presentation configuration with named press/hover/focus/select/reveal/
  route/attention/commit/error/result/boss recipes and one semantic FX palette.
- Replaced the payload-free cue seam with source/target/amount/tone/receipt feedback events.
- Added an interruption-safe motion director, dynamic target registry, reusable pointer/focus
  manipulator, identity-aware staggered card/choice reveals, and a bounded Painter2D FX plane for
  pulses, route arcs, and travelling Sand grains.
- Added the Last Hour semantic theme layer, cold-iron scrollbar treatment, visible focus states,
  and five code-native Painter2D station sigils (no platform emoji).
- Added a dedicated F1 `UI FX` tab: live nested controls, editable ranges, Save/Reload, and
  Reveal/Select/Commit/Route/Attention/Error previews. F2 Flow Lab carries the same preview seams
  plus a presentation contract.
- Audio and haptic sinks are structurally ready but intentionally no-op until authored cues and a
  platform provider land.
- Unity verified: source import/compile clean; Hall route + presentation contracts; five vector
  sigils and UI FX tab present; rapid route/selection spam settles; desktop Table/Market captures;
  forced phone and reduced-motion paths; final console 0 warnings / 0 errors.

## Combat spectacle — Jake approved everything, P0-P4 BUILT same evening

Jake on the go-big menu: *"honestly i love them all, but we can def shelf lower prio stuff
like 10 until later"* → this arc = core + proposals 1/2/3/4/5/9; shelved 6/7/10; 8 later.
Full asset batch approved. Five agents built in sequence, every phase editor-verified
(console clean + probe captures + contact sheet run twice → 28/28 byte-identical PNGs):

- **P0 sim** (`f2ea2f4`, 313 tests): Burn fold divergence FIXED with failing-then-passing
  guardrail (found a second half live: the merge site emitted deltas — now absolute pool);
  StatusApplied stamps TicksLeft/SwingsLeft into Aux2/Aux3; fold tuple carries ExpiryTick
  (hash-excluded — countdown is decoration); AbilityIdentity.Resolve in Warband.Content
  (last SignatureOverride wins); TellMatch byAbility at +2 specificity; replay v4→v5,
  fixtures+DLLs regenerated atomically. First commit in a while whose tree builds standalone.
- **P1 FX foundation** (`3c7ab1a`): VfxLibrary C# recipes (14), VfxInstance pooled +
  Director-stepped (seeded Simulate, root-only), 6 hand-HLSL URP shaders (Ring/GroundFill/
  Sigil/Glow/Particle/Dissolve, zero `_Time`), TellDef vfx/projectileVfx/impactVfx/groundVfx
  + byAbility + hitAnim/announce/pulseGround, ProbeShots harness (probes.txt-driven targeted
  captures). Whole client compiled headless against Unity 6000.3.19 reference assemblies
  (found in Shoota's LinuxServer build — the p1check scratch project is reusable).
- **P2 fields** (`4841f59`): FieldView per field — boundary ring rim, per-hex scrolling
  floors (per-hex phase offset), spawn = edge-then-floor, event-driven pulse envelope,
  expiry fade, wall = grey slab + base rim, _fieldBornAt for true field age in frozen
  scrubs. Flat repainted hexes gone.
- **P3 status icons** (`6590382`): pip cubes → billboarded icon rows: procedural family
  glyphs (ALL control = hourglass variants — the Hour owns control), stacks (dots / ×N
  crisp text / Burn pool), countdown ring vs playhead (drain visible in frozen captures by
  construction), priority control-leftmost-1.2×, cap + "+N" chip.
- **P4 casts** (`14d3d4b` sigils + `d205c6a`): 8 era sigil masks generated (flux-2-dev,
  512², approved batch — cleric's has a pentagram-ish star, regen if it reads wrong);
  26 byAbility cast rows (the closed resolvable set), cast-aura-<chassis> recipes, lane
  colors + tier glows (defensives below bloom), castSound riser seam, announce rationed
  twice (9 S-crown rows + per-caster 6s cooldown → 0.08-0.40 lines/s from 2.27 worst).
  Fixture units all resolve to override ids (pyromancer.inferno etc.) — byChassis rows
  are stock-build fallbacks now.

**In flight:** P5 death pipeline (Hit/Death animator states, DeathSequence linger +
dissolve, ash-death marks + grave props — proposal 1). **Deferred to Jake live:** field
idle brightness (F1 fieldIdleAlpha), statusIconSize, wall tint, field floors read as coins
not hex-edged fills, HP-bar snap vs 0.5s T3 windups (bar tween if it reads wrong).
**Editor hygiene note:** found the editor left in Play Mode by another session mid-arc —
exited before frozen captures; probe runs during that window were discarded/redone.

## Combat spectacle — ARC COMPLETE (P5, P6, all assets; nine commits total)
- **P5 death presentation** (`6fd1a06`): Hit/Death animator states (one-shot idempotent
  editor script, .controller committed) · DeathSequence: hit-freeze → Death_A slump →
  ember dissolve (per-texture cached WarbandDissolve, MPB cutoff) → **ash-death** (proposal
  1): permanent ash mark + the unit's weapon cloned as a grave marker. Loop-wrap fully
  restores (explicit Idle replay — Death state holds by design). Probes: Phalanx mid-slump
  t25, pike planted as grave at +1.25s, two graves + kill lines at t26.
- **P6 assets** (`14d3d4b` sigils · `d18399c` risers): 8 era sigils + 8 era windup stings
  (castSound seam live). GenerateAsset gotcha: retry stubs/dupes — cleaned over SSH at
  C:\Dev\game\warband (the MCP relay blocks destructive file ops).
- **P6 dress** (`f074a1c`): Trigger-rider echo row (0.7× numbers, spark-link root→target;
  Source==Root on 64/64 fixture riders) + Counter reversed-grammar row · **Deathless dress**
  — 0.4s hold + key light −40%, survivor's red rune flare the only bright thing +
  DEATHLESS feed line (probes: stomp t25 is the showcase — Deathless at t25, same
  Berserker dies t26 into the fight-ender) · fight-ender 0.2× slow-mo from the folded
  side-wipe fact + vignette · camera LAW: explicit bigImpact rows, position-only push-in
  (billboard quaternion stays valid), shake rationed 1-per-3s (Starfall+Death same beat →
  Starfall takes it).
- **Every phase gated identically:** headless client compile (p1check vs Shoota's Unity
  reference assemblies — new standing capability, see memory), console/asset probes,
  event-derived ProbeShots captures, contact sheet ×2 → 28/28 byte-identical.
- **For Jake's live pass:** fight-ender + camera punch/shake feel (play-mode only) · riser
  mix + announce density in motion · field idle brightness / icon size / wall tint /
  cleric sigil star (F1) · HP-bar snap vs T3 windups (bar tween if wrong) · P6 authoring
  note: Heal carries no Cause (Boon pulses dormant — one-line sim change when wanted).
