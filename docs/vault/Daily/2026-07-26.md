# 2026-07-26

## Opening Muster — icon-first decision system

Replaced the first run screen's universal `WarbandCard` adaptation with a dedicated desktop
Muster system.

- `MusterCardModel` allows exactly Health, Basic power + cadence, Reach, Signature, and Passive.
  It cannot accidentally inherit weapon footers, flavor paragraphs, prices, rank, or arbitrary
  stat rows.
- Added a code-native `WarbandGlyph` family for roles, scan stats, and kit keywords. Plain roles
  and keywords live in `UnitPresentation.json`; all mechanical numbers still come from the
  composed `UnitDef`.
- Added an in-portrait disclosure lens. Pointer hover is scoped to each fact/rule; keyboard focus
  opens a combined exact view. Weapon name, Mana per swing, crit, and cleave live only in the
  lens.
- `MechanicalRulePresenter` now owns base-attacks-to-signature math and full innate/passive
  language. Focused tests lock the opening cycles at Cleric 4, Bulwark 2, Shade 7, Sharpshot 5,
  Pyromancer 4, Berserker 3, Phalanx 4, Banneret 3.
- Replaced the numeric 0/3 chip with three ordered portrait sockets. Select/deselect transactions
  resolve before rebind; a full warband blocks replacement with “Remove one champion first.”
- Added tunable reveal, lens, select, deselect, blocked, ready, and socket-compaction timings to
  `HubPresentation.json`, reflected by F1. F2 exposes screen, fresh offer, lens states, transactions,
  blocked, ready, and validation previews.
- Added `IRunScreenLifecycle`: leaving Muster cancels every pending lens/reveal and clears the FX
  plane, preventing presentation from leaking over the following screen.

Mobile-specific composition remains deferred by explicit project direction. Verification target:
1920×1080 and 2560×1440, including initial, lens, 3/3, blocked, and reduced-motion captures.

## Overnight (Jake asleep) — gap analysis, then act bosses + disclosure end to end

Jake, before bed: *"research our game, do some gap analysis on what features or content or design
ideas/combat rendering we are missing. Make a priority list (things not in the roadmap yet) and get
things into the roadmap — THEN implement something for me end to end while I sleep."*

### Gap analysis → board items 7-15
Nine items new to the board, verified against code rather than remembered, prioritised P1-P4.
**P1 (silently blocks friends playtest #1):** no run save/resume — CONTINUE is memory-only and the
only thing written to disk about a player is `ui.reducedMotion`, while losses are already terminal ·
**no standalone build has ever been made**, and `Shader.Find` will silently drop all six combat
shaders in a player build without Always Included Shaders · no player-facing options (mute, speed,
motion). **P2 (item 1's real target, cheap):** the impact `punch` balloon (0.750 → ~1.03, measured,
hides HP bars — a `tuning.json` value) · **overtime renders as literally nothing** — the Storm ramps
from tick 900 with no clock and no tell, so a long fight reads as "units started dying". **P3:**
enemy disclosure depth · the endless seam does not exist. **P4:** acts 2 and 3 draw an identical
node pool · the Interlude is a one-button non-choice. Full reasoning on the board.

### Built: ADR 0024 — per-act bosses + the disclosure contract
Picked the top *content* gap that is fully headless-verifiable (the Unity editor is a shared
singleton and Jake's focused editor is needed for captures). It turned out to be two problems that
had to ship together.

- **Act bosses.** Act 1 keeps The Last Oath (only boss with measured evidence). Act 2 **The Ashfall
  Battery** — Rooted gun behind two Colossi, shells your FARTHEST unit, leaves a burning crater, so
  the reflex "bunch behind the tank" loses. Act 3 **The Waning Crown** — bell fed by time AND by
  every death in its court, so clearing escorts is what rings it. Bosses take no act curve; the
  multiplier survives only past act 3 for endless, which keeps act 1 numerically identical and the
  oath probe's evidence valid.
- **The disclosure half was the bigger find.** `BuildFightBeat` hardcoded "THE LAST OATH" and
  disclosed nothing for the four node encounters, and `UnitCardFromDef` titles enemy cards from
  `ContentLexicon.Chassis(ChassisId)` — so an **Hourling previewed as "Shade" with the Shade's
  ability text**. ADR 0023 had already declared ChassisId a render key; the shell was treating it as
  identity. Brief and spawn now come from one method, the brief carries every body with a behavior
  sentence, and enemy cards use authored names and no portrait.
- **`--boss`, a new instrument.** Holds a boss to a harder bar than `--enc`: not win%, but *how many
  kinds of strength can pass it* (four parties — balanced/reach/control/damage). It caught the act-2
  boss posing nothing on its first run (three axes at 100% from **every** formation, spread 0); the
  bell went 14s → 9s against that measurement. Nice confirmation in the same table: act 2's control
  axis shows 50% rule-fired because Warden's Taunt Silences the gun and a Silenced clock never
  fires — the disclosed answer, observably working.
- **Gates:** 392 tests · scenarios round-trip and byte-stable across two runs · DLLs rebuilt · whole
  client compiled headless against Unity 6000.3.19 reference assemblies, with the gate itself
  negative-controlled (injected a type error, confirmed it was caught).
- **Not eyes-verified.** Two fixtures (`boss-ashfall-battery`, `boss-waning-crown`) were added via a
  new `encounter` seam in `scenarios.json` so either boss can be watched immediately. Contact sheet
  is now **10 fixtures, not 8**.
- **The bosses are not where runs die.** `--enc`'s naive line after the change: bot completes 3/12,
  and 7 of the 9 deaths are at **act 2 NODE fights** (4× at node 0 alone) — one at an act-2 boss,
  none at the act-3 boss. So act 2's node pool, not the new exams, is the wall. Relevant to board
  item 4 (pressure tiers) and item 14 (act identity); do not "fix" the bosses on this number.

## Roadmap grooming pass + item 7 (save/resume)

Jake asked for a grooming/priority pass, then "build it" on the outcome.

**Grooming.** Item 1 restaged **DESIGN→BUILD ⇒ VERIFY**: it contained a straight contradiction (the
beat sequencer called unbuilt in one paragraph, shipped in another) and all three of its failure
candidates have now been built against with none of it watched — so its blocker is Jake, not a
session. Gave it a four-thread index (1a spectacle VERIFY · 1b Hall polish · 1c Phase 4 **half**
built, `BattleForecast` has zero client references · 1d camera). Item 5's two "still scaffolding"
claims were false after ADR 0023/0024 and four of its seven remaining bullets were done → dissolved
to a laws page, residue pointed at items 2④/13/16. Item 10 gained the exact knob
(`impact.punchBoost`, global, default 0.8 — one F1 slider, not 197 tell rows).

**Jake's three decisions:** ① the content budget cap is **three acts, one boss per act** (it had said
"one act / do not expand", contradicting ADR 0019) — which promotes item 14 to real work; ② next
build order **7 → 8 → 9**; ③ **terminal loss stays**, save/resume is the mitigation, and act 2's node
pool is explicitly not to be tuned down for run length.

**Built item 7.** `RunSave` (in Warband.Run, no file IO — the run layer is pure, so the host owns the
bytes and the format stays headless-testable) + `RunController.Resume` (regenerates nothing, resolves
every content id eagerly so an old save fails with the id named) + client `RunSaveFile`
(temp-then-move, never throws, deletes what it cannot read) + autosave on `Rebuild()`, the shell's
single choke point. Hand-rolled `dotted.key=value` format because Warband.Run has zero package
references by law and IL2CPP strips reflection. **412 tests.** The one that matters: a run saved,
serialized and rebuilt from text plays out *identical* to one never saved — same battles
event-for-event.

**Verified on Windows, and one permanent constraint found.** New committed harness
`Assets/Editor/RunSaveCheck.cs` (menu `Warband/Verify Run Save`, edit-mode, MCP-drivable) proved the
real platform path: save at `AppData/LocalLow/InhouseBoyz/Warband`, bytes survive Windows text IO,
no CR in the record separator, resume matches on every axis. 12/12 PASS, console clean.
Then: **`EditorApplication.EnterPlaymode()` is refused over MCP** — *"User interactions are not
supported for MCP tool calls."* **Play Mode is unreachable from any session, permanently.** So button
wiring and anything frame-driven is Jake-only verification. The workable substitute is exactly what
`RunSaveCheck`/`RenderShots` do: a committed edit-mode Editor script driven by `ExecuteMenuItem`
against the real DLLs. Added to the client gotchas. Item 7's shell wiring is therefore built,
compile-checked and platform-verified at the save layer, but **not clicked through**.

### The thing to fix first tomorrow: the tree was not committed
The working tree held **178 uncommitted files**, including the entire ADR 0022 + ADR 0023
implementation — `Enemies.cs`, `EncounterProbe.cs`, `MechanicalRulePresenter.cs`, the unit-behavior
and signature-patch tests — plus most of the client shell. All of it is listed as **Done** on the
board while being absent from git. Direct consequence: `--enc` now reports three of four node
encounters as FREE at their debut act, contradicting ADR 0023's recorded numbers, and it **could not
be bisected because no baseline existed**. Everything is committed now.

## Server question → the content version stamp

Jake: *"do we have a server set up? I assume we will want some kind of cloud storage especially if we
end up adding PvP aspects back yeah?"* → then *"not something that needs to be built today. let do
it"* on the one prerequisite.

**Answer.** No server, and zero networking in the codebase. But homeserv already runs Caddy, Docker,
Tailscale split-DNS and a native Palworld server, and Shoota has a Unity LinuxServer build — so
hosting is a solved problem and deferring costs nothing. The clarification that matters: **every PvP
idea in the vault is asynchronous** (Echo exhibitions vs a stored snapshot, leaderboards, ghosts).
That is "upload a blob, download a blob" — a key-value store behind Caddy, not netcode. Real-time PvP
would be a different game, and nothing in ADR 0016 asks for it.

**The real gap, built.** ADR 0008 has specified `contentVersion` since 2026-07-22 and it never
existed — `Replay.cs` had a format version only. Now `IRunContent.ContentVersion`, a **computed**
FNV-1a-64 fingerprint of the whole content graph, not a hand-bumped constant (the failure mode is a
retune, which is exactly what a human forgets to bump). Stamped into `RunState` and `GhostSnapshot`,
checked on resume.

**Why the id check wasn't enough** — worth remembering, because it looks redundant: `Resume` already
resolves every id, so a *rename* fails loudly. But encounters derive from the seed at FIGHT time, so
identical ids with different numbers resume happily and then fight a different army than the save was
made against. No symptom. The hasher therefore walks the whole graph — selectors, conditions, fields,
pulses, mastery riders, patches — and a test proves a retune is caught at 11 different depths. A
shallow hash would have been worse than none: a guarantee it doesn't keep.

**Deliberately not done:** replays are not stamped. They store the full event log, so playback is
content-independent; bumping the format to v6 would force regenerating 10 fixtures for a reason that
does not hold. Re-simulation is what content changes break — saves and snapshots, which are stamped.

**Verification:** 433 tests. The algorithm is pinned to an independently-computed digest so nobody can
"simplify" it to `string.GetHashCode()` (per-process randomized → saves fail after restart, looking
exactly like corruption). Cross-machine: `Warband.Content.dll` is byte-identical on homeserv and
Windows. **Left undone:** the on-Windows harness run — `RunSaveCheck` was extended and synced, then
**Codex took the Unity lock** mid-verification. Not retried in a loop (per the lock protocol). Run
menu `Warband/Verify Run Save` when it frees; homeserv-side fingerprint for comparison is
`f1f4a7e9b5cd527b`.

## Unified decision cards + Hall browse/detail stage

Jake asked to make the opening Muster grammar the default language for the shops: semantic styling,
flat Signature Mana cost, compact stock with price as its only scan number, and a full selected-card
stage for everything exact.

Built one presentation contract with four density profiles (Feature / Stock / Detail / Target), a
typed fact registry, code-native glyphs, canonical labels/colours/tooltips, and advanced help for
the Mana-per-hit → attacks-to-ready calculation. Muster now shows the Signature's flat Mana cost;
Market stock is portrait/name/category/price; the persistent right stage owns stats, rules, rank
comparisons, choice previews, and actions. Warband/Armory/Hourstone use the same compact target/stock
grammar. Detail swaps use a cancellable tuning recipe exposed by the reflection-built F1 cockpit.

The live pass paid for itself. It caught four issues static review did not: a Crit qualifier forced
visible by inline style · hidden rule copy still tripping the old full-card validator · compact
Warband/Hourstone abilities overriding their profile and threatening the footer · a glyph lookup
that failed on the second station bind because `WarbandGlyph.Set` changes the element name. It also
caught global selection fallback seating a hero dossier in an empty Hourstone. Selection is now
station-scoped and empty stations use the full workspace.

**Verification:** clean Unity compile; `Warband/Verify Decision Cards`; live Muster containment;
Market split, title, and commerce bounds at 2542×1304; repeated
Market→Warband→Armory→Hourstone→Warband rebind; compact Target containment; empty Hourstone; and
Hourstone→Market return all passed. Game View capture confirmed the overall hierarchy and semantic
fact styling. Mobile-specific composition remains deliberately deferred.

## Item 8 — first standalone build, launcher, delivery (piggybacked on Shoota)

Jake: *"get to work on it. look at what we did for shoota too, it would be great for play testing if
we built a launcher and content delivery system, we can piggy back off that infra if needed."*

Read Shoota's pipeline first and copied its shape rather than inventing one: `deploy/ship-release.sh`
(pull → verify → zip → atomic publish), `launcher/main.go` (manifest → download → SHA-256 → atomic
install → launch), the `app.json`/Makefile target conventions. warband has no server, so the server
half is simply absent.

**The landmine was real — and the preflight caught it before a build was spent on it.** New
`Warband/Build Preflight` reported all six hand-HLSL shaders as "would be STRIPPED from a player
build". `GraphicsSettings.asset` held seven always-included shaders, every one a Unity built-in. All
six warband shaders are reached only through `Shader.Find` and referenced by no material asset, so
the first friend build would have silently lost the entire combat-spectacle arc — and it would have
looked like broken FX code, not a build setting. `WarbandBuild` now registers them itself on every
build and logs what it added; verified afterwards in `GraphicsSettings.asset` by mapping guid → meta.

**Built:** v0.1.260726.1352 · 162 MB · 0 errors · content `f1f4a7e9b5cd527b`. Publish produced a
58 MB zip + launcher manifest. Launcher verified end to end against a local HTTP server (cold
install, hash verify, atomic install with rollback, version marker, the stand-in exe genuinely ran,
second run short-circuits to "Up to date"), cross-compiles to a 6.1 MB Windows PE32+ binary.

**Two design changes vs Shoota's launcher, both deliberate:** the token is **optional** (Shoota's Go
site checks the header and hard-fails without it; warband serves a static manifest, so the gate can
be added later without reissuing launchers) and the zip URL is **resolved relative to the manifest
URL**, which means a plain static Caddy directory is sufficient — no application server at all.

**Closed an open question:** the content fingerprint computed inside Unity on Windows is
`f1f4a7e9b5cd527b`, identical to homeserv's. That was left unverified when the version stamp landed.

**Gotcha that cost a detour — worth remembering.** The built player appeared to create a `run.save`
on a cold boot, which would have meant the autosave hook was firing when no run existed. It is not:
deleting the save and relaunching produced none. The cause is that **the Editor and the built player
share `Application.persistentDataPath`** — the same directory holds `run.save` and the Editor's own
`Unity/…/Editor/Analytics`, so a Play Mode session (Codex was doing station verification earlier)
writes the file a friend build would read. Benign, but it is exactly why a fresh build can look like
it "already has a run".

**Not verified:** whether the built player boots to the menu and renders. Batchmode launches over SSH
produced no player log and no visible process, and a session cannot screenshot a player window. One
double-click by Jake settles it. Two sudo steps and the gate decision are also his —
`deploy/README.md` §"STILL NEEDS JAKE".
