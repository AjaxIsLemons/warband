# 2026-07-28

## Persistent Warband footer — roster drag and drop

The shell-owned footer can now manipulate the roster instead of only inspecting heroes and
transferring gear.

- `RunController.MoveRosterHero` addresses the source by stable hero identity and the destination
  by zone + visible slot. An open slot moves the hero into that packed roster; an occupied slot
  swaps atomically, including field↔reserve when both zones are full. Planning/spec-choice guards
  remain authoritative in the run layer.
- `WarbandBarView` owns pointer capture, a seven-pixel drag threshold, source/target/ghost states,
  invalid-drop cancellation, and legal highlighting. Green open slots mean move; blue occupied
  slots mean swap. Ordinary clicks still open the dossier.
- Keyboard parity: Space arms a focused hero and places/swaps it on the next legal slot; Space on
  the source or Escape cancels. Tooltips disclose the interaction.
- The footer projection now carries an explicit `SlotIndex`, independent of the field-only index
  used by deployment.
- Added the deterministic `rail-open` fixture, including an editor-only drag preview and roster
  interaction report.

Verification so far: `Warband.Run.Tests` **239/239 PASS** (four focused roster-move cases included);
`make check-client` **PASS, 0 errors across 59 scripts**; `git diff --check` clean. Unity import,
console, and the `rail-open` capture remain the final gate because `unity-warband` was held by
another active session at the first verification attempt.

> **Note from the overnight workbench session:** `EditorRosterInteractionReport`'s
> `open > 0` requirement failed every at-capacity warband (8 occupied / 0 open — a legal, common
> state), which painted 60+ false FAILs across the Workbench matrix. Corrected to accept
> at-capacity rosters (swap targets still exist); `rail-open` still exercises the open-slot path.
> Your capture gate has since run green inside the workbench matrix (roster interaction PASS on
> all 70 items, `rail-open` included).

---

## The Workbench dossier & armory-drawer redesign (overnight, Jake's directive — roadmap item 24)

Jake, heading to bed: *"the dossier information in general is quite crowded … per-type formats
for hero/trinket/weapon/inscription cards … remove the armory tab, keep it like a drawer on the
footer … research, and spend as much time as needed to get this right."*

### Research first (three parallel Opus agents)
① Autobattler shops (TFT, HS Battlegrounds, Super Auto Pets, Backpack Battles, Mechabellum,
Underlords) ② card anatomy (StS, Balatro, Monster Train, Wildfrost, Inscryption, PoE, WoW, D4)
③ progressive disclosure (CK3, Factorio, LoL, NN/g, Material bottom sheets, Hades' boon-list
failure). Decision doc: **`Design/workbench-dossier.md`**. Headline diagnosis, confirmed in
code: the card layer and keyword layer were already genre-correct — **the dossier was the one
broken layer, because section demotion was decided by pixel width and array index**
(`Recruit && i >= 2`; paginate under 1000px) — the exact failure Super Auto Pets shipped in
0.28 and Backpack Battles patched away.

### What shipped
- **`InspectorSectionRole` (Primary/Deferred)**: role decides what renders, width only decides
  layout. Deferred = one compact row (icon · trigger label · name · full rule on hover) — never
  hidden. The pager/secondary-column/width-pagination machinery is deleted.
- **Per-kind dossiers**: Recruit/Champion lead with the SIGNATURE; basic attack one-liner;
  passive deferred. Weapon = profile + comparison, mastery deferred. RankUp = before→after gain
  rows (`HEALTH 164 → 188 +`) + B/A/S ladder + two first-sentence path previews (full rule on
  hover; the +N stat chips are deleted — one fact, one channel). Equip projection = deltas own
  everything (rule sections defer, stat chips clear). The fight-side card is explicitly
  `Combatant` now and keeps its everything-primary layout.
- **Two-column dossier** (identity column | detail column) under `.wb-inspector--workbench`;
  the synthetic "CARD TYPE" passive back-fill is gone.
- **Armory side column deleted** (190px closed / 790px open — open also hid the Market). Now a
  **footer drawer band above the unit rail**: peek strip `ARMORY · N ITEMS · OPEN DRAWER ▴` →
  184px tile band; Market and rail stay live throughout (the equip flow needs item and socket
  visible together — Hades' documented mistake).
- Riding along: comparison-cell overlap ("PO4WER → 19") · `accent--choice` no-op ·
  `stilledbell` dead `"control"` accent · footer RESERVE mid-word wrap · the roster-contract
  correction above.

### The verify loop (eleven matrix rounds, all read by eye)
R1 65 fails (mostly the roster contract) → R2/2b **stalled at "1/70 load": the matrix leaves
play mode running, so a later refresh defers its recompile and the relaunch runs OLD assemblies
against NEW uxml — the view constructor throws on a renamed element, readiness never comes
true, and the console shows nothing** (Editor.log over SSH was the honest record; recovery:
cancel + ExitPlaymode + wait + relaunch — memorized) → R3 54/70 → R4 51/70 (my own
`flex-shrink: 0` on sections killed their width shrink — vertical pinning ≠ horizontal) →
R5 67/70, and the capture exposed the ghost behind five rounds of identity-column weirdness:
**HallPhysicalStyles' `.wb-inspector__summary { justify-content: center }` — centered overflow
spills both ways** (title climbing into the portrait, stats falling past the clip) → R6/R7
that fix + drawer tiles (percent-height resolves to 0 against a min-height:0 flex grid; rely
on stretch) + projection identity slimming + RESERVE label fix → R8 the **stat rail moved from
the identity column (which never had the height for it) to the top of the detail column** —
which exposed the pre-existing `line-copy { min-height: 64px }` block as the hidden height hog
→ R9/R10 the drawer-open dossier made ALWAYS compact (rules defer, chips yield — browsing or
projecting) → **R11 FINAL: 68/70.**
**Residual, accepted + documented:** `market-recruit @ 2556×1317` ×2 — the subtitle
wrapped-text check reports 31.4 > 23.9 while the rendered capture shows the full one-line
subtitle intact; a measurement artifact at that scale factor, not a visible defect. Final
captures: `client/TempCaptures/ui-qa/20260728-025334/`.

### Morning follow-up (Jake's review, built while he ran errands)
Jake's two asks: long ability prose gets cut off (repro: Phalanx rank-up → Pikewall), and
the armory peek strip should go — the footer's "N STORED / drop to unequip" chip should be
the drawer handle.
- **Rank-option previews now show the authored lexicon one-liner** (`ContentLexicon.Node`),
  not the generated trigger-prose. Pikewall's machine rule is 174–354 chars with no sentence
  break — `FirstSentence` couldn't split it and the card guillotined it mid-line. The full
  mechanical rule stays on hover and in the blocking choice; the `FirstSentence` fallback is
  budget-clamped at a word boundary for any model without a summary. `market-rankup-long`
  is now the REAL Phalanx fork built from live catalog content (`Kits.Nodes` +
  `MechanicalRulePresenter` + lexicon), so the existing wrapped-text contract finally
  guards real prose instead of fixture-sized copy.
- **Peek strip deleted; the footer ARMORY chip is the only drawer handle** (what the design
  doc specified — the strip was overbuild). Chip hint is state-driven: `DROP TO UNEQUIP`
  while gear is armed/dragged, else `OPEN DRAWER ▴` / `CLOSE DRAWER ▾`. Click toggles
  open/close on the Workbench (read-only screens keep the navigate-home behavior).
- Riding along: dossier cost's last digit clipped at 1024×768 (hub's 206px economy floor
  vs the 196px identity column — pre-existing, visible in last night's captures too);
  footer "RESERV/E" mid-word wrap (label now sizes to text, nowrap); rank-up market card
  cost/art/accent now patched to match the fixture dossier.
- **Tooling: the "idle player loop" mystery is SOLVED.** The matrix only ever worked
  unfocused because Unity happened to hold OS focus overnight. `Application.runInBackground
  = true` (now pinned in `WarbandUiQa.Begin`/`Tick`; resets on every play entry) makes play
  mode free-run unfocused — a full 70-item matrix now takes ~70 s. The OFF state fails
  LOUD (every contract NaN, captures time out 30 s/item), so a green run is trustworthy.
- Verify: three matrix rounds this morning — poisoned run (frameCount stuck at 1) →
  68/70 → 66/70 (my +10px reserve-label fix pushed the 8-tile rail out of bounds at
  1024×768; reverted to size-to-text) → **68/70 final** (`TempCaptures/ui-qa/20260728-091422`),
  the 2 remaining rows being the documented 2556×1317 subtitle measurement artifact.
  Phalanx/Cleric rank-ups, armory chip states, cost digit, and RESERVE label all read
  correct by eye at 1600×900 and 1024×768.

### Open / for Jake
- The in-motion feel pass is yours: drawer open/close, hover cadence on deferred rows, the
  rank-up read. Item 1's play pass now includes this surface.
- Deferred follow-ups (in the session's card-anatomy notes + item 24): weapon-tier "augmented"
  value marking on a non-hue channel · WHEN/THEN trigger anatomy for trinkets/inscriptions ·
  compact-card text-budget CI assertion · hero rank pips · paradox-inscription badge ·
  `MarketOfferCardModel.Qualifier` dead slot.
- Dead code flagged, deliberately untouched: ManagementView/ShopView/PlanningView (~2,300
  lines) unregistered since the view-table refactor; WarbandCard/CardRulesPopover only
  reachable from them. Separate cleanup decision.

---

## Owed-capture sweep — 5b post-bridge verify + item 11's Waning (late morning)

The Unity lock had gone stale, so the two machine-verifiable leftovers were closed in one hold:

- **Item 11, the owed overtime capture:** edit-mode `BuildPreview` at t=800 → `THE WANING IN
  0:10` (warning gold), t=950 → `THE WANING — 2/TICK` (storm red) — pixels and the live
  world-TextMesh inventory both match the hand-checked formula, confirming the `_clock = tick`
  fix in the capture path itself. PNGs: `client/McpCaptures/verify5b/`.
- **5b, world-rail absence:** `hourstone` at t=24/t=200 shows no law-name badges anywhere; the
  only world texts are StatusIconRow's `count`/`chip`, the story feed, and the clock (verified
  by name against `StatusIconRow.cs`). ReplayPlayer.cs synced byte-identical to Windows first.
- **5b, post-bridge smoke matrix: 17/17 PASS** (`TempCaptures/ui-qa/20260728-093053/`), six
  captures read by eye at 1280×720 + 1600×900. The two pre-existing `rail-full` header/Market
  overlaps (the 13/15 from the tray session) now PASS — item 24's header/market reshape fixed
  them en route. Tray verified collapsed-and-clean on Workbench (full/open/tooltips), Wager,
  Deploy, and the Result gate — the recap capture doubles as the persistent-layer proof.
- Editor left in edit mode, console cleared, play mode exited. Roadmap updated (5b + item 11).

**5b and item 11 are now Jake-only:** drawer hover cadence, TriggerFired flash/indicator feel,
and the Waning in motion — all fold into item 1's pass.

---

## Item 9 — the options screen (midday, Jake away)

The last P1 blocker on friends playtest #1. Built as **a modal over the shell's persistent
layer** — one implementation serves Menu, Hall and fight — rather than a routed screen, because
mid-fight is where mute and battle speed are actually wanted and routing away from a fight is
worse than a scrim over it.

- **`PlayerOptions.cs`** (new): PlayerPrefs store; the only new state. Sound on/off,
  Master/Interface/Battle volumes, reduced motion (reuses the existing `ui.reducedMotion` key),
  battle speed 0.5–2×. `ApplyAudio()` pushes the mixer params; mute = MasterVol −80 dB so the
  per-surface enable seams stay shipped defaults.
- **`OptionsPanel.cs`** (new): scrim + `.modal`, applies instantly, no OK/Cancel. Volume rows
  disable when sound is off. Carries its own `EditorResolvedLayoutReport` (controls asserted
  against the modal, per the result-gate lesson).
- **Entries:** OPTIONS button on the menu · OPTIONS beside SKIP on the fight overlay · Esc
  toggles everywhere (documented collision: Esc during an armed keyboard drag also opens it).
- **Battle speed** multiplies tuning's `playback.ticksPerSecond` at ReplayPlayer's two read
  sites; a live fight re-reads through `ReapplyTuning()` (the F1 path). Render-only.
- **Verified:** `make check-client` 61/0 · smoke matrix extended to an `options` surface —
  **18/18 PASS**, capture read by eye (`ui-qa/20260728-095256/`) · **mixer param names checked
  against the real GameMixer with a negative control** (a wrong name is a silent no-op — the one
  failure mode the sliders could not afford; BogusParam correctly failed) · scrim pixel-measured:
  0.78 alpha in LINEAR space, consistent with every existing modal.
- **Jake-only remainder:** click the menu/fight buttons, hear the sliders, feel a live speed
  change and the reduced-motion rebuild. Folds into item 1's pass.

---

## Item 19 — run telemetry (afternoon; Jake spec'd it from his phone)

Jake's call, verbatim spirit: *"log every fight, every purchase, every tier selection."* Built the
same afternoon. Full state on the board (item 19); the short form:

- `Warband.Run/RunTelemetry.cs` formats one JSON line per event under RunSave's law (pure, no
  packages, hand-rolled writer); 5 headless tests verify it against System.Text.Json as the
  independent parser. Run id = seed + content prefix — stable across save/resume, tested.
- RunShell hooks every decision seam, all fail-silent; `RunTelemetryWriter` appends beside
  `run.save` and uploads the finished run fire-and-forget.
- `site/runlog.go`: key-gated, size-capped POST → one JSONL file per UTC day. Smoke-tested
  locally end to end (auth 404s, 204 append, 413 oversize, file verified).
- Tree committed at both stream boundaries (`cc058c2` = the 07-27/28 wave, then item 19).
- **Open:** deploy (`make site-deploy`) awaits Jake's tap; one real Play-Mode run writing and
  uploading is the remaining verify.

---

## Item 29 — enemy board identity (evening; "just build, ask when needed")

The last remnant of item 2: enemy CARDS stopped borrowing hero names/portraits weeks ago, but the
BODIES on the board were still the hero mini the ChassisId points at. An Hourling was a Rogue, an
Ashen Colossus was a Knight. Five roles that pose five different problems arrived as five familiar
champions.

**The seam.** `ChassisId` is a render key pointing at a hero; the role is what the body IS, and it
never reached the client. Added `UnitDef.RoleId` → `PlaybackUnit.RoleId` → replay **v9** → hashed
in `HashView`, so the round-trip test proves the wire carries it. The stamp lives in
`Encounters.Place()`, not at a catalog resolution point — the first cut stamped in `Catalog` and
`Encounters.ById` (which feeds every render fixture and probe) silently kept shipping roleless
bodies. The fixtures were the thing being verified, so that would have been a self-confirming pass.

**The bodies.** Seven authored silhouettes from the existing primitive vocabulary, no art
dependency: Swarm small/hunched/cheap · Anchor a boxy monolith, the widest mass on the board ·
Artillery leaning back over a splayed base behind a raised barrel · Ritualist a legless rooted
column · Diver pitched forward mid-pounce · Siege and Hour as the bosses' big-brother grammar.
Plus per-role ground tells: the **artillery firing line** pointing downrange, and the **ritual
clock** — a disc at the Ritualist's foot that fills with mana, because a rooted ritual never
attacks and was the most dangerous thing on the board with no board-level tell at all.

**What the captures caught (none of it visible from the code):**
- Marks parented to `Body` inherited its lean and punch-scale — they tipped off the floor and would
  have pumped on every hit. They belong on `Root`, which owns position only.
- The Diver's blades came out as horizontal handlebars: they are authored in body space, which
  already carries the 30° pitch, so "swept back" had to be raked back *on top of* it.
- The Anchor read white, not enemy-red. Chased it as a specular blowout (switched the torso to a
  box) before querying the material: `_BaseColor` was correct red and the applied value was
  **(1,1,1)** — it was mid tell-flash, because my probe list passed `advance 0.0`, which freezes
  every tell at maximum. **Capturing at advance 0 makes body colour unreadable** — worth knowing
  before the next render item chases the same ghost.

**Verified:** 522 tests (2 new role-plumbing tests, 1 guarding the authoring layer directly) ·
`make check-client` PASS · `make scenarios` round-trips all 14 fixtures on v9 · two new fixtures
`enc-ninth-bell` (Anchor + Swarm + Ritualist) and `enc-the-drop` (Diver + Artillery) so all five
node roles are capture-covered, bosses supply Siege + Hour · contact sheet run four times:
**B/C/D byte-identical across all 56 frames**, only the first pass differed (first-render warm-up).

**Jake's call, on the board's play-pass list:** procedural monsters as the placeholder language, or
roles back on hero minis with role kit. Captures in `tmp/item29/`.

---

## Session 3 — item 22: the board is 8×8 (ADR 0027)

Jake nodded the design in chat (Portal): 8×8 at ~45° pitch, semantic formation remap, camera
defaults from the sim-render audit's dial point. Built same-session.

**The change:** `Battle.BoardCols` 6→8 · board dims join the content hash (a dims-only retune can
never silently diverge a save again) · every authored formation remapped by the ADR's column law
{0→0,1→1,2→3,3→4,4→6,5→7} with hand-fixes where the map broke identity (Slagworks/Battery walls hug
the center — the LANE is the identity; the Oathbound pair keeps its 5-col gap, centered) · probes
translated/re-anchored · `Formation.ColOrder` center-out for 8 · scenario fixtures +1 col ·
camera fov 34 / pitch 42 / yaw 6 / distance 1.45 (all F1 sliders).

**Verified:** 522 tests · scenarios round-trip · `make check-client` PASS · golden replay
round-trips · baseline re-measured, drift published in the Done line.

**The finding (design backlog):** the a1 boss admits 2 answer axes, was 3 — control fails the Last
Oath at EVERY placement tried, including the literal 6-wide coordinates of both sides. The width
itself flipped it. Not tuned, per doctrine; Jake decides (re-author · amend guard · wait for play).

**Untested:** the actual pixels of the new framing — DLLs + tuning + fixtures are in the Syncthing
payload; first Play Mode session on the wide board is Jake's. The camera numbers are dial starting
points, not commitments.

---

## Independent game-flow assessment + three-month roadmap recut

Jake asked for roadmap curation from the game itself, explicitly without letting the current board
choose the answer. The assessment walked the live shell from menu through draft, Hall, wager,
auto-seeded deployment, and first replay; read the current capture corpus; ran all 522 headless
tests; and re-ran the encounter and boss probes before consulting the roadmap.

**Finding:** the build already has enough combinatorial material to test the thesis (8 heroes,
78 live spec nodes, 11 weapons, 5 trinkets, 12 Inscriptions, 6 encounter families, 3 bosses).
The strategic shell is substantially more finished than its battlefield payoff, and no human has
completed the current end-to-end run. Three node families probe free/flat for purpose-built parties
while the naive line completes 1/12 runs; bosses are mostly strong formation exams. The investment
call is therefore conversion and validation, not more content.

Jake chose a **three-month slice**, a **45–60 minute standard run**, a **full replacement of
Now/Next**, and proof by **one Jake full run plus one cold friend attempt**. The live board now
orders: combat payoff · pacing/economy · encounter differentiation/difficulty · one authored event
· Beyond the Hour endless seam · dead-code cleanup/slice stability. Old passive/muster render
items folded into combat; generic Workbench/Hall polish moved behind feedback. No client or sim
source changed in this curation session.

---

## One-command launcher release

Jake asked for one homeserv command that updates the downloadable game. Added `make release`:
headless tests → `unity-sim` DLL rebuild → Syncthing completion → `unity-warband` lease → MCP
request into the already-open Windows Editor → request-scoped build-status polling → existing
manifest/content-version guarded atomic ship → public release-status check.

The Editor build queues through `WarbandBuild.QueueWindowsClientBuild(requestId)` and reports to
`%USERPROFILE%/warband-builds/build-status.json`, outside the repository. This avoids both unsafe
second-Editor batch mode and MCP's long-call timeout; the old `release.json` is removed before the
queue call returns, so the publisher cannot mistake a stale build for the requested one.

**Camera dial (same session, Jake handed over the Unity lock):** four probe-shot iterations
against live editor renders. The audit's dial point (dist 1.45) clipped the front rank — at 42°
pitch the near half projects far taller than the far half, and distance alone can never center the
trapezoid. Added `CameraTune.aimBias` (look-at pulled toward the near edge, ~4 lines, F1-reflected
automatically). **Final: fov 34 · pitch 42 · yaw 6 · distance 1.6 · aimBias 0.2** — full board in
frame on Waning Crown and overtime captures (`tmp/item22-camera/final-*.png`). Discovered en route:
the kill feed clips at the right edge on the wide board → folded into item 30. NB the roadmap was
re-cut by Codex's curation session mid-build (items 30–32); item 32 owns the a1-boss third-answer
restoration now.

---

## Item 28 — dead-view cleanup

Reference audit confirmed that `RunShell.BuildViews` registers `MenuView`, `RecruitView`,
`WorkbenchView`, `WagerView`, `DeployView`, and `RunOverView`; the old `ManagementView`,
`ShopView`, and `PlanningView` had no construction path. Deleted those three views plus the
dead `WarbandCard` and `CardRulesPopover`, along with their `ManagementHall`,
`PlanningWorkspace`, and `WarbandCard` UXML trees: **3,105 source lines removed**. Two unreachable
`ManagementView` editor fallbacks were removed from `RunShell`.

Five live renderers were borrowing only `WarbandCard.SetAccent`. That eight-class toggle moved to
`DecisionCardPresentation.ApplyAccent`; no accent vocabulary or styling changed. The retained
`PlanningWorkspaceStyles.uss` still owns shared base/card interaction styles, so only its stale
header was corrected.

**Verified:** `make check-client` PASS (57 scripts, 0 errors) · all **522** sim/run tests pass ·
Syncthing 100% · Unity asset refresh confirmed all eight dead assets absent and let Unity remove
their `.meta` files · cleared console stayed at 0 warnings/errors · `Warband/Build Preflight`
reported every check PASS / READY TO BUILD. Editor remained in Edit Mode and the shared lease was
released.

---

## Revisions — watched timeline split (ADR 0028)

Jake accepted time as the game's player-owned combat differentiator, with two constraints that
shaped the implementation: no attack-clock manipulation, and more expressive choices than Mana
alone. Built the full vertical slice.

The pure layers now prepare fights without advancing the run, preserve one immutable opening,
simulate the watched future provisionally, and commit exactly one original or revised outcome.
The branch is re-simulation, not replay editing. `TimelineIntervention` lands before ordinary
actions at the selected tick; consequences remain ordinary Move/Mana/Shield/Status events, with
`RevisionApplied`, `UnitOmitted`, and `UnitReturned` as presentation boundaries. Missing Hour's
Omitted state is alive for victory but outside targeting, occupancy, effects, actions, personal
rules, and status-clock decay.

Content complete:
- **Borrowed Future** base + Shared Premonition / Deep Reserve, Clear Intention / Long Memory,
  Convergence / Afterthought.
- **Recall to Formation** base Disarm (never Silence) + Fixed Point / Long Peace, Roll Call /
  Empty Hands, General Recall / Missing Hour.
- First Draft after Muster shows both full trees. Each Interlude blocks first on the next authored
  two-option Revision evolution, then shows its normal reward. Save/resume carries lineage and
  ordered upgrades; no Sand or Workbench progression is involved.

The fight overlay offers one proactive split after a full observed second, or freezes immediately
before a losing fatal event if the charge remains. It uses whole-second anchors (1–4, or 6 with
Long Memory), Cast/Death timeline landmarks, board target rings, local playhead pause, desaturated
held-Hour dress, reverse fold reconstruction, Reduced Motion, and a branch landing beat. Mouse/
touch, keyboard, and gamepad actions share the same controller path. Telemetry records lineage,
upgrades, anchors, targets, proactive/final use, and outcome flips.

Flagship presentation pass: the first open now arrests the mix and fractures the full screen around
a sand-gold clock; cancel/reopen gets a short recall rather than replaying the reveal. Commit runs
tear → 1.35–1.85 second continuous reverse fold → vacuum → lineage landing → readable receipt on an
unscaled clock. Borrowed Future lands mana-blue; Recall lands ash-violet; return-from-Missing-Hour
has its own punctuation. The receipt compares authoritative original/revised folds and only names
proven Mana, Shield, status, position, omission, or return changes. A dedicated highest-priority
Revision mixer bus and deterministic 11-clip sand/glass/bell source family back the sequence.
Reduced Motion crossfades to the branch, removes shake, and preserves the receipt.

**Verified:** 281 sim + 252 run = **533 tests** · rebuilt Unity runtime assemblies ·
`make check-client` PASS · Unity import/compile and console clean (0 warnings/errors) ·
1920×1080 First Draft and held-Hour UI captures reviewed · actual Play Mode
open→target→commit Borrowed Future branch PASS · build preflight invoked · Editor returned to
Edit Mode and `unity-warband` lease released.

### Revision fracture flagship follow-up

Replaced the temporary UI fracture with a native URP RenderGraph pass after transparents and before
post-processing. The pass preserves the witnessed future in a cross-frame RT, folds it against the
live branch on one side of a migrating fault, and ships full-resolution PC plus half-resolution
mobile capture paths. The visual hierarchy is now a single black temporal chasm, thin sand-gold
rims, broad displaced/graded plates, restrained subsidiary cracks, opening impact bloom, reverse
sand, and target-connected lineage rings on landing. `RevisionFractureLab` supplies deterministic
phase scrubbing, dual-time setup, a 22-frame capture matrix, renderer installation, and a state-
lifecycle contract check.

**Verified:** headless client compile PASS · Unity import/compile clean · shader supported with two
passes and zero shader messages · PC/Mobile renderer features installed with Full/Mobile quality ·
22-frame matrix recaptured over the live `castfest` board · Unity console 0 warnings/errors.

## Interlude selection review (evening)

Jake flagged the Interlude blocking choice as broken (`docs/ui-reviews/inbox/interlude-cleanup/`):
seven undifferentiated strips, colliding titles, one-word-per-line rules, three-way path structure
invisible, established card grammar discarded by `WorkbenchView.Choice()`. Job
`interlude-cleanup` opened; three coded-mockup directions rendered at 2554×1303 in
`docs/ui-reviews/outbox/interlude-cleanup/samples/` (01 grouped modal · 02 dedicated beat screen
with seal-commit · 03 workbench rail + inspector reuse). AWAITING_REVIEW — no client changes yet.

## Evening — item 33: Workbench column refactor (design → approved → BUILT → verified)
Jake opened with a reference screenshot (`docs/ui-reviews/inbox/workbench-refactor/`) and four
directives: columns not rows, dossier right at ~30%, compact the header, bigger rail cards with
ability + tier-up slots, kill the market heading strip. Ran the ui-review flow: shopfront vs
ledger mockups → shopfront chosen → r2 (dossier PATH section, offer tier strips, armory rack
popup) → style stabs → **obsidian picked** → approved `05-shopfront-obsidian` with one
condition (node map stays in the header). Built the same evening, client-only: Workbench
UXML/USS rewritten, InspectorPanel permanent narrow form + PATH (real content: `SpecOptions`
B/A/S, fork rank named), MarketOfferCard tier strips, WarbandBarView progression cards,
floating rack on the existing drawer state, contracts rewritten. Verification: nine batches
against the 70-item matrix (0→12→17→64→69→**70/70 ×3** + rank-up regression + by-eye).
Decisive finds: Unity default Label margins (~4px top+bottom) were most of the overflow; the
base sheet's partial-width `wb-inspector__column` silently narrowed all rule text (eye-caught,
contract-blind); Syncthing "valid" ≠ file arrived (now verify via editor-side read — memory:
`unity-matrix-drive-gotchas`). One Unity crash mid-evening (restart + clean relaunch). 4:3
keeps the slim rail card. NOTE for the interlude-cleanup session: its three mockups were
rendered against the OLD workbench — direction 03 (rail + inspector reuse) should re-render
against the new column layout before Jake picks.

## Late — battle-environment art effort opened (lore-first)
Jake opened "make the sim render world interesting" → scouted the render reality (the whole
battle world is `ReplayPlayer.BuildBoard()`: Plane + 64 flat fans, 4-object scene, solid-color
void; render-polish.md's diorama direction never built) → the design chat went lore-first and
produced the **salvage spine** (candidates, NOT locked → `Design/theme.md`): expeditions
salvage dying eras (heroes/gear/laws/sand) to feed a Tower that powers *something* against the
end; the collapse has an author (act bosses = its manifestations, name open); act wins extract
an era Key beyond sand (fiction on the existing BossReward beat now; any real meta layer is
post-slice and can't be account power); battlefield = the last shard of a dying era scried
through the Hourstone; tone leaning melancholy. Board: new **item 35 (DESIGN)** — the shard
frame, staged procedural-frame → dressing → per-act eras; KayKit Medieval Hexagon (CC0, same
author as the minis) proposed as one era's kit, pending nod.

**Then Jake said "lets get to building!" → Stage 1 BUILT the same night.** New
`ShardEnvironment.cs` (fractured cliff skirt ring-built off the board rectangle, void gradient
dome via URP/Unlit + baked 1×256 ramp, far-side debris, Tower silhouette rising from the deep,
"~env-*" fill/Sand-rim rig — KeyLight skips the prefix); beveled tile mesh + seeded value
jitter in BuildBoard; `EnvironmentTune` (26 fields → free F1 sliders + hot reload); tile-dress
hash folds tileScale/bevel/variation/seed into ApplyBoardTune's rebuild test (kills the
"slider needs a hexSize nudge" quirk). Four probe rounds: r1 exposed bright rim trim, Sand
wash killing enemy tile tint, near-quadrant debris on the lens, clipped Tower; r2–r4 fixed
all (rim ring tucked under the plane, rim light 38°/0.28, debris ±75° far arc, Tower
recomposed as cropped colossus). Gates: headless compile PASS ×3, contact sheet
**byte-identical ×2 (57 frames)**, console 0/0. Discovered + memorized: play camera sees
25°–59° BELOW horizontal — backdrop art must rise from the void, horizons are invisible.
Uncommitted (tree shared with other sessions' WIP); unwatched in motion → play-pass.
